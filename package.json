{
  "name": "blog-project",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "validate:articles": "node scripts/validate_articles.ts articles",
    "validate:articles:distribution": "node scripts/validate_articles.ts articles --phase=distribution",
    "test": "node --test",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "dependencies": {
    "fs-extra": "11.4.0",
    "gray-matter": "4.0.3"
  },
  "devDependencies": {
    "@types/fs-extra": "11.0.4",
    "@types/node": "24.13.3",
    "typescript": "7.0.2"
  }
}
